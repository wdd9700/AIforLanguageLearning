# 安全认证模块依赖
# 安装命令: pip install -r requirements.txt

# FastAPI 框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# 密码哈希 - 使用 bcrypt (with automatic salt)
bcrypt>=4.1.0

# JWT 处理
python-jose[cryptography]>=3.3.0

# 数据验证
pydantic[email]>=2.5.0

# Redis 支持 (optional, falls back to memory if not available)
redis>=5.0.0

# Python 版本要求: >= 3.11
